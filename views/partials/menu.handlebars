<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">Sonos Control</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                {{#if currentUser.permissions.home}}
                    <li class="{{{isMenuItemActive 'home' page child}}}"><a href="/">Home</a></li>
                {{/if}}
                {{#if currentUser.permissions.presets}}
                    <li class="{{{isMenuItemActive 'presets' page child}}}"><a href="/presets/">Presets</a></li>
                {{/if}}
                {{#if currentUser.permissions.settings}}
                    <li class="dropdown {{{isMenuItemActive 'settings' page child}}}">
                        <a class="dropdown-toggle" data-toggle="dropdown" href="/settings/">Settings
                            <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li class="{{{isMenuItemActive 'setpresets' page child}}}"><a href="/settings/setpresets/">Setup Presets</a></li>
                        </ul>
                    </li>
                {{/if}}



                {{#if currentUser.permissions.logout}}
                    <li><a href="/logout/">Logout</a></li>
                {{/if}}
            </ul>
        </div>
        <!--/.nav-collapse -->
    </div>
</nav>